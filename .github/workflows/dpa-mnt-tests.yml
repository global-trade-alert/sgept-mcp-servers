name: dpa-mnt tests

on:
  push:
    paths:
      - 'dpa-mnt/**'
      - '.github/workflows/dpa-mnt-tests.yml'
  pull_request:
    paths:
      - 'dpa-mnt/**'
      - '.github/workflows/dpa-mnt-tests.yml'

jobs:
  pytest:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dpa-mnt
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: latest
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Run pytest
        run: uv run pytest -v --cov=dpa_mnt --cov-report=term-missing
